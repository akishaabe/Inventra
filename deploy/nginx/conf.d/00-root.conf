server {
    listen 80 default_server;
    # Apex/root domain and catch-all
    server_name cafe-inventra.com www.cafe-inventra.com _;

    # Simple redirect to the shared app (login/landing)
    return 302 http://shared.cafe-inventra.com$request_uri;
}
