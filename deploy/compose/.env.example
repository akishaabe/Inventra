# Copy this file to /opt/Inventra/.env on the server (or to this same folder as .env for local compose)
# Then run: docker compose -f /opt/Inventra/deploy/compose/docker-compose.prod.yml up -d

# -------------------------
# MySQL container initialization
# -------------------------
# Root is only used internally by the MySQL image on first start; do NOT set MYSQL_USER=root
MYSQL_ROOT_PASSWORD=irino
MYSQL_DATABASE=inventra
MYSQL_USER=inventra
MYSQL_PASSWORD=@Inventra123

# -------------------------
# Backend DB connection
# -------------------------
# Use the Docker Compose service name for host
DB_HOST=mysql
DB_NAME=inventra
DB_USER=inventra
DB_PASSWORD=@Inventra123
PORT=4000

# -------------------------
# ML service DB connection (optional)
# -------------------------
ML_DB_HOST=mysql
ML_DB_NAME=inventra
ML_DB_USER=inventra
ML_DB_PASS=@Inventra123

# -------------------------
# Cross-Origin settings for the SPAs
# No spaces; no trailing slashes
# -------------------------
ALLOWED_ORIGINS=http://admin.cafe-inventra.com,http://supad.cafe-inventra.com,http://staff.cafe-inventra.com,http://shared.cafe-inventra.com

# -------------------------
# SMTP for email verification codes
# -------------------------
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=teaminventra@gmail.com
SMTP_PASS=ubyiowyynfgkcuum
FROM_EMAIL=teaminventra@gmail.com

# -------------------------
# ML service URL (internal service name + port)
# -------------------------
ML_SERVICE_URL=http://ml_service:5000

# -------------------------
# JWT (currently unused; placeholder to silence warnings)
# -------------------------
JWT_SECRET=placeholder
